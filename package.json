{
  "name": "@cyotee/opencode-marketplace",
  "version": "0.1.0",
  "description": "Marketplace support for OpenCode: install Claude-format plugin catalogs as native OpenCode components.",
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": "./dist/plugin.js"
  },
  "main": "./dist/plugin.js",
  "bin": {
    "opencode-marketplace": "./dist/cli.js"
  },
  "files": ["dist", "src"],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun build ./src/plugin/index.ts --outfile dist/plugin.js --target node && bun build ./src/cli/index.ts --outfile dist/cli.js --target node",
    "typecheck": "tsc --noEmit",
    "prepack": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^20.0.0",
    "bun-types": "^1.3.14",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@opencode-ai/plugin": "*",
    "@opencode-ai/sdk": "*"
  }
}
